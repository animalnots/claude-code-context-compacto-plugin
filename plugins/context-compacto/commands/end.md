---
description: Set tail-window token budget (raw token count). E.g. /cc:end 30000.
argument-hint: "N"
---

Print ONLY the output below verbatim. No commentary, no preamble, no trailing remarks.

```
!`python "${CLAUDE_PLUGIN_ROOT}/hooks/pcconf.py" end $ARGUMENTS`
```
